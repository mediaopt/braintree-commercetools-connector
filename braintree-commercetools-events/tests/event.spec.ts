import { describe, expect, test } from '@jest/globals';

describe('Testing braintree-commercetools-events Controller', () => {
  test('POST `/braintree-commercetools-events` route', async () => {
    expect(1).toBe(1);
  });
});
