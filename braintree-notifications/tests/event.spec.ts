import { describe, expect, test } from '@jest/globals';

describe('Testing braintree-notifications Controller', () => {
  test('POST `/braintree-notifications` route', async () => {
    expect(1).toBe(1);
  });
});
